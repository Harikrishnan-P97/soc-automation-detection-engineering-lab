# SQL Injection Attempt Detection

## Overview

This detection identifies SQL Injection (SQLi) attack attempts against the web application by analyzing ModSecurity audit logs generated by the OWASP Core Rule Set (CRS).

The rule extracts HTTP request metadata, source information, triggered WAF rule details, and attack outcomes to provide analysts with the context required for investigation and response.

The detection was validated using attack simulations against DVWA (Damn Vulnerable Web Application) within the SOC lab environment.

---

## Detection Logic

The detection monitors ModSecurity audit logs for events categorized as SQL Injection attacks.

When a matching event is identified, the search extracts:

* Source IP address
* HTTP request method
* Requested URI
* User-Agent string
* Triggered ModSecurity rule ID
* HTTP response status code
* Attack outcome (Blocked or Allowed)

Additional metadata is added to support alert triage, investigation, dashboarding, and automation workflows.

---

## MITRE ATT&CK Mapping

| Tactic         | Technique                                 |
| -------------- | ----------------------------------------- |
| Initial Access | T1190 – Exploit Public-Facing Application |

---

## Alert Metadata

| Field           | Value                          |
| --------------- | ------------------------------ |
| Alert Name      | SQL Injection Attempt Detected |
| Severity        | Critical                       |
| Risk Score      | 90                             |
| Alert Type      | Web                            |
| Asset Type      | Web Application                |
| Event Source    | ModSecurity                    |
| Index           | waf                            |
| Web Application | DVWA                           |

---

## SPL Query

```spl
index=waf sourcetype=modsecurity:json
| eval src_ip='transaction.remote_address'
| eval transaction_id='transaction.transaction_id'
| eval uri='request.request_line'
| rex field=uri "^(?<http_method>\w+)\s+(?<clean_uri>[^\s]+)"
| eval user_agent='request.headers.User-Agent'
| eval message=mvindex('audit_data.messages{}',0)
| eval full_msg=mvjoin('audit_data.messages{}'," ")
| rex field=message "\[id \"(?<rule_id>\d+)\"\]"
| rex field=full_msg "code (?<status_code>\d+)"
| search message="*SQL Injection*"
| eval alert_name="SQL Injection Attempt Detected"
| eval severity="critical"
| eval risk_score=90
| eval alert_type="web"
| eval asset_type="webapp"
| eval mitre_tactic="Initial Access"
| eval mitre_technique="T1190"
| eval webapp="DVWA"
| eval dest_host='transaction.local_address'
| eval index_name="waf"
| eval event_source="ModSecurity"
| eval event_id=rule_id
| eval alert_id="WAF-" . rule_id . "-" . strftime(_time,"%Y%m%d%H%M%S")
| eval timestamp=strftime(_time,"%Y-%m-%d %H:%M:%S")
| eval attack_outcome=if(status_code="403","blocked","allowed")
| table _time alert_id timestamp alert_name severity risk_score alert_type mitre_tactic mitre_technique src_ip dest_host webapp http_method clean_uri status_code attack_outcome user_agent message event_source event_id index_name transaction_id
| sort - _time
```

---

## Detection Workflow

1. An attacker submits a malicious SQL payload to the DVWA application.
2. ModSecurity inspects the HTTP request.
3. OWASP CRS identifies the payload as a SQL Injection attempt.
4. ModSecurity generates an audit log entry.
5. Splunk ingests the WAF telemetry.
6. The SQL Injection detection search identifies the event.
7. An alert is generated and forwarded to the SOC automation pipeline.
8. Analysts can investigate the alert through Grafana dashboards.

---

## Validation Method

The detection was validated using multiple SQL Injection payloads against DVWA.

Example payloads:

```sql
' OR 1=1 --
```

```sql
admin'--
```

```sql
' UNION SELECT NULL,NULL,NULL --
```

```sql
1' OR '1'='1
```

During testing, OWASP CRS generated ModSecurity alerts which were successfully ingested and detected by Splunk.

---

## Sample Alert Output

The alert provides analysts with:

* Source IP Address
* Requested URI
* HTTP Method
* User-Agent
* ModSecurity Rule ID
* Attack Outcome
* MITRE ATT&CK Mapping
* Risk Score
* Investigation Context

Screenshot:

```text
../../screenshots/splunk/SQL-Injection-Detection-Splunk.png
```

---

## Detection Use Cases

This detection supports:

* Web Application Attack Monitoring
* Threat Hunting
* SOC Alert Triage
* Incident Investigation
* MITRE ATT&CK Mapping
* Security Dashboard Visualizations
* Alert Enrichment and Automation

---

## Related Components

* ModSecurity
* OWASP Core Rule Set (CRS)
* DVWA
* Splunk Enterprise
* n8n SOAR Workflows
* Grafana SOC Dashboards

---

## Notes

This detection relies on ModSecurity and OWASP CRS signatures rather than direct payload matching, allowing the WAF to identify a wide variety of SQL Injection techniques while reducing the need for custom detection logic.

All testing was performed inside a controlled lab environment for educational and defensive security purposes.
